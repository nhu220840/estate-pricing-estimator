# Render Blueprint: https://render.com/docs/blueprint-spec
# Dashboard → New → Blueprint → chọn repo → Render đọc file này.

services:
  - type: web
    name: tinix-valuation
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    region: singapore
    healthCheckPath: /health
